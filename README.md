# Auto Card

Pure command-line battle simulator for the rogue-like card game MVP.

## Run

```bash
uv run python main.py
uv run python main.py --enemy guard --seed 104
uv run python main.py --enemy priest --seed 2
```

## Test

```bash
uv run pytest
```
